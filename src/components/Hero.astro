---
import { profile } from '../data/profile';
---

<header id="top" class="hero">
  <!-- Soft aurora that eases toward the cursor (no 3D, very light) -->
  <div class="hero-aurora" aria-hidden="true">
    <span class="blob b1"></span>
    <span class="blob b2"></span>
    <span class="blob b3"></span>
    <span class="blob b4"></span>
  </div>
  <div class="hero-grain" aria-hidden="true"></div>

  <div class="container hero-inner">
    <p class="hero-eyebrow reveal">{profile.role} · {profile.location}</p>
    <h1 class="reveal">
      Hi, I'm <span class="name">{profile.name}</span>.
    </h1>
    <p class="hero-tagline reveal">{profile.tagline}</p>
    <p class="hero-sub reveal">
      I build systems that learn — from large language models and retrieval pipelines
      to the infrastructure that runs them.
    </p>

    <div class="hero-actions reveal">
      <a href="#projects" class="btn btn-primary">View my work</a>
      <a href="#contact" class="btn">Get in touch</a>
    </div>

    <div class="hero-socials reveal">
      <a href={profile.links.github} target="_blank" rel="noopener" aria-label="GitHub">GitHub</a>
      <a href={profile.links.linkedin} target="_blank" rel="noopener" aria-label="LinkedIn">LinkedIn</a>
      <a href={profile.links.twitter} target="_blank" rel="noopener" aria-label="Twitter / X">Twitter</a>
      <a href={profile.links.email} aria-label="Email">Email</a>
    </div>
  </div>

  <a href="#about" class="scroll-cue" aria-label="Scroll to content">
    <span>Scroll</span>
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M19 12l-7 7-7-7"/></svg>
  </a>
</header>

<script is:inline>
  (() => {
    const aurora = document.querySelector('.hero-aurora');
    if (!aurora || matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    let tx = 0.5, ty = 0.4, cx = 0.5, cy = 0.4;
    addEventListener('pointermove', (e) => {
      tx = e.clientX / window.innerWidth;
      ty = e.clientY / window.innerHeight;
    }, { passive: true });

    const tick = () => {
      cx += (tx - cx) * 0.05;   // ease toward the pointer
      cy += (ty - cy) * 0.05;
      aurora.style.setProperty('--mx', cx.toFixed(4));
      aurora.style.setProperty('--my', cy.toFixed(4));
      requestAnimationFrame(tick);
    };
    tick();
  })();
</script>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  /* ── Aurora ─────────────────────────────────────────────────────────── */
  .hero-aurora {
    position: absolute; inset: -10%;
    z-index: 0;
    filter: blur(70px) saturate(118%);
  }
  .blob {
    position: absolute;
    border-radius: 50%;
    opacity: .82;
    mix-blend-mode: multiply;
    will-change: transform, translate;
    /* Each blob drifts toward the cursor by a different amount (parallax depth).
       `translate` (mouse) composes on top of `transform` (the idle float). */
    translate:
      calc((var(--mx, .5) - .5) * var(--px, 80px))
      calc((var(--my, .5) - .5) * var(--py, 80px));
  }
  .b1 {
    width: 50vw; height: 50vw; top: -10%; right: -2%;
    --px: 130px; --py: 100px;
    background: radial-gradient(circle at 50% 50%, var(--aur-1), transparent 66%);
    animation: float1 17s ease-in-out infinite;
  }
  .b2 {
    width: 44vw; height: 44vw; top: 18%; right: 16%;
    --px: -100px; --py: 80px;
    background: radial-gradient(circle at 50% 50%, var(--aur-2), transparent 66%);
    animation: float2 21s ease-in-out infinite;
  }
  .b3 {
    width: 42vw; height: 42vw; bottom: -12%; right: 24%;
    --px: 80px; --py: -120px;
    background: radial-gradient(circle at 50% 50%, var(--aur-3), transparent 68%);
    animation: float3 19s ease-in-out infinite;
  }
  .b4 {
    width: 38vw; height: 38vw; top: 26%; right: -8%;
    --px: -150px; --py: -70px;
    background: radial-gradient(circle at 50% 50%, var(--aur-4), transparent 68%);
    animation: float1 23s ease-in-out infinite reverse;
  }
  @keyframes float1 { 0%,100% { transform: translate(0,0) scale(1); } 50% { transform: translate(-4%, 5%) scale(1.06); } }
  @keyframes float2 { 0%,100% { transform: translate(0,0) scale(1); } 50% { transform: translate(5%, -4%) scale(1.08); } }
  @keyframes float3 { 0%,100% { transform: translate(0,0) scale(1); } 50% { transform: translate(3%, 4%) scale(1.05); } }

  /* keep particles from crowding the headline on the left */
  .hero-grain {
    position: absolute; inset: 0; z-index: 0; pointer-events: none;
    background: linear-gradient(102deg, var(--bg) 12%, color-mix(in srgb, var(--bg) 45%, transparent) 40%, transparent 64%);
  }

  /* ── Content ────────────────────────────────────────────────────────── */
  .hero-inner { position: relative; z-index: 1; max-width: 820px; padding-top: 4rem; padding-bottom: 4rem; }
  .hero-eyebrow {
    font-size: .82rem; font-weight: 600; letter-spacing: .16em; text-transform: uppercase;
    color: var(--text-muted); margin-bottom: 1.6rem;
  }
  .hero h1 {
    font-size: clamp(2.9rem, 8vw, 5.4rem);
    font-weight: 500;
    letter-spacing: -.04em;
    line-height: 1.0;
  }
  .name { font-weight: 600; }
  .hero-tagline {
    font-size: clamp(1.25rem, 2.6vw, 1.9rem); font-weight: 400;
    margin-top: 1.6rem; color: var(--text);
    letter-spacing: -.02em;
  }
  .hero-sub { font-size: 1.12rem; color: var(--text-soft); margin-top: 1rem; max-width: 40rem; }
  .hero-actions { display: flex; gap: .8rem; margin-top: 2.4rem; flex-wrap: wrap; }
  .hero-socials { display: flex; gap: 1.6rem; margin-top: 2.4rem; }
  .hero-socials a {
    font-size: .9rem; font-weight: 500; color: var(--text-muted);
    border-bottom: 1px solid transparent; transition: color .2s, border-color .2s;
  }
  .hero-socials a:hover { color: var(--text); border-bottom-color: var(--text); }

  .scroll-cue {
    position: absolute; bottom: 1.8rem; left: 50%; transform: translateX(-50%);
    display: inline-flex; align-items: center; gap: .5rem;
    font-size: .72rem; font-weight: 600; letter-spacing: .14em; text-transform: uppercase;
    color: var(--text-muted);
  }
  .scroll-cue svg { animation: bob 2s ease-in-out infinite; }
  @keyframes bob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(5px); } }

  @media (max-width: 860px) {
    .hero-aurora { inset: -20%; filter: blur(60px) saturate(115%); }
    .hero-grain { background: linear-gradient(180deg, transparent, var(--bg) 86%); }
    .blob { opacity: .5; translate: none; }
  }
</style>
